name: Meu Primeiro Workflow


on:
  push:
    branches: [ main ]

jobs:
  exemplo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o código do repositório
        uses: actions/checkout@v4

      - name: Rodar um comando de teste
        run: echo "O GitHub Actions está funcionando com sucesso!"
